# Native Core Skeleton

This crate captures core protocol types required by the implementation plan:
- install stage model
- final status model
- managed block marker protocol
- path template variable resolution for Windows targets

Windows adapter and SQLite registry implementation should be added in subsequent tasks.
